replicaCount: 2

image:
  repository: yourdockerhubusername/game-backend
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  host: game-backend.local
